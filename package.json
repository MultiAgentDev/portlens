{
  "name": "portlens",
  "version": "1.0.1",
  "bin": {
    "portlens": "./packages/cli/dist/index.js"
  },
  "scripts": {
    "build": "npm run build --workspace=packages/cli",
    "prepare": "npm install --workspace=packages/cli --ignore-scripts"
  },
  "workspaces": [
    "packages/cli",
    "packages/shared"
  ],
  "devDependencies": {
    "typescript": "^5.5.0"
  },
  "type": "module",
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
